<!DOCTYPE html>
<html lang="en">
<head>
    <title>Email Verification - RK Customs</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- Boxicons for icons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    
    <link rel="icon" type="image/png" href="/assets/images/logo3.png">
</head>
<body style="user-select: none; -webkit-user-select: none; -webkit-user-drag: none;">
<div class="min-h-screen bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 flex items-center justify-center p-4">
  <div class="max-w-md w-full">
    <!-- Logo -->
    <div class="text-center mb-8">
      <%= image_tag "logo4.png", alt: "RKCustoms", class: "h-16 w-auto mx-auto mb-4" %>
      <h1 class="text-2xl font-bold text-white">Email Verification Required</h1>
    </div>

    <!-- Verification Card -->
    <div class="bg-zinc-800/50 backdrop-blur-sm rounded-2xl border border-zinc-700 p-8 shadow-2xl">
      <!-- Status Icon -->
      <div class="text-center mb-6">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-yellow-500/20 rounded-full mb-4">
          <i class="bx bx-envelope text-3xl text-yellow-400"></i>
        </div>
        <h2 class="text-xl font-semibold text-white mb-2">Verify Your Email Address</h2>
        <p class="text-zinc-400 text-sm">
          We've sent a verification link to <strong class="text-white"><%= @user.email %></strong>
        </p>
      </div>

      <!-- Instructions -->
      <div class="bg-zinc-700/30 rounded-xl p-4 mb-6">
        <h3 class="text-white font-medium mb-2 flex items-center">
          <i class="bx bx-info-circle text-blue-400 mr-2"></i>
          What to do next:
        </h3>
        <ul class="text-zinc-300 text-sm space-y-1">
          <li class="flex items-start">
            <span class="text-blue-400 mr-2">1.</span>
            Check your email inbox (and spam folder)
          </li>
          <li class="flex items-start">
            <span class="text-blue-400 mr-2">2.</span>
            Click the verification link in the email
          </li>
          <li class="flex items-start">
            <span class="text-blue-400 mr-2">3.</span>
            You'll be redirected to your dashboard
          </li>
        </ul>
      </div>

      <!-- Resend Button -->
      <div class="space-y-4">
        <%= form_with url: resend_verification_email_path, method: :post, local: true, class: "w-full" do |form| %>
          <button type="submit" class="w-full bg-gradient-to-r from-yellow-500 to-orange-500 hover:from-yellow-600 hover:to-orange-600 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:ring-offset-2 focus:ring-offset-zinc-800">
            <i class="bx bx-refresh mr-2"></i>
            Resend Verification Email
          </button>
        <% end %>

        <!-- Cooldown Info -->
        <% if @user.verification_sent_at && Time.current < @user.verification_sent_at + 2.minutes %>
          <div class="bg-yellow-500/10 border border-yellow-500/20 rounded-xl p-3">
            <p class="text-yellow-400 text-sm text-center">
              <i class="bx bx-time mr-1"></i>
              Please wait <%= ((@user.verification_sent_at + 2.minutes) - Time.current).to_i %> seconds before requesting another email
            </p>
          </div>
        <% end %>

        <!-- Help Text -->
        <div class="text-center">
          <p class="text-zinc-400 text-xs">
            Didn't receive the email? Check your spam folder or try resending.
          </p>
        </div>
      </div>

      <!-- Back to Login -->
      <div class="mt-6 pt-6 border-t border-zinc-700">
        <div class="text-center">
          <p class="text-zinc-400 text-sm mb-3">Need to use a different email?</p>
          <%= link_to logout_path, method: :get, class: "text-yellow-400 hover:text-yellow-300 text-sm font-medium transition-colors duration-200" do %>
            <i class="bx bx-log-out mr-1"></i>
            Sign out and try again
          <% end %>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="text-center mt-8">
      <p class="text-zinc-500 text-xs">
        Having trouble? Contact support for assistance.
      </p>
    </div>
  </div>
</div>

<!-- Background Animation -->
<div class="fixed inset-0 -z-10 overflow-hidden">
  <div class="absolute -inset-10 opacity-50">
    <div class="absolute top-1/4 left-1/4 w-72 h-72 bg-yellow-500/10 rounded-full mix-blend-multiply filter blur-xl animate-pulse"></div>
    <div class="absolute top-3/4 right-1/4 w-72 h-72 bg-orange-500/10 rounded-full mix-blend-multiply filter blur-xl animate-pulse animation-delay-2000"></div>
    <div class="absolute bottom-1/4 left-1/3 w-72 h-72 bg-red-500/10 rounded-full mix-blend-multiply filter blur-xl animate-pulse animation-delay-4000"></div>
  </div>
</div>
</body>
</html>
